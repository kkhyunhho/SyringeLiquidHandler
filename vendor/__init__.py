"""Vendored third-party drivers that aren't pip-installable upstream.

Currently: `linear_motor_controller` (MINAS A6 linear rail, codename `lmc`),
copied from coport-uni/LinearMotorController. Kept in-repo so the cell runs
standalone; re-copy the source to update.
"""
