# Python dependencies for cv_mass_measurement.py.
# Install into the active env:  pip install -r requirements.txt
# Requires Python >= 3.12 (sy01b's floor).
#
# The two device controllers (entris_ii, sy01b) are NOT listed here — they
# are imported directly from the sibling project repos' src/ dirs via the
# sys.path bootstrap at the top of cv_mass_measurement.py, not pip-installed.

pyserial>=3.5    # serial link to the balance (entris_ii) and pump (sy01b)
openpyxl>=3.1.5  # .xlsx result workbook (pulls in et-xmlfile)
